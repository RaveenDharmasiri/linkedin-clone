import styled from "styled-components";

import Header from "./common/Header";

const Home = (props) => { 

    return(
        <div>
            <Header />
            Home
        </div>
    );

}

export default Home;